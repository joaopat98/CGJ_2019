#include <iostream>
#include "vectors.h"
#include "matrix.h"

using namespace std;

int main() {
	/*
	mat3 m1 = mat3(
		1, 2, 3,
		4, 5, 6,
		7, 8, 9
	);

	mat3 m2 = mat3(
		5, 2, 0,
		-1, 4, 1,
		0, 0, -6
	);

	//sum
	mat3 m3 = m1 + m2;

	//subtraction
	mat3 m4 = m1 - m2;

	//multiplication
	mat3 m5 = m1 * m2;

	mat3 m6 = m1 * 4;
	mat3 m7 = 4 * m1;

	vec3 v1 = vec3(1, 2, 3);
	vec3 v2 = m1 * v1;

	//transpose
	mat3 m8 = m1.transpose();

	//determinant
	int det1 = m1.determinant();

	//inverse
	try {
		mat3 m9 = m1.inverse();
	}
	catch (NonInvertableException& e) {
		cout << "m1 is not invertable!" << endl;
	}

	//identity
	mat3 m10 = MatrixFactory::identity3();
	mat3 m11 = m10 * m1;

	//dual Matrix
	mat3 dual = MatrixFactory::dualMat(v1);
	vec3 v3 = vec3(4, 2, 1);
	vec3 v4 = dual * v3;

	*/
}